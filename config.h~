#define MODKEY Mod4Mask

static const char *termcmd[] = { "st", NULL };
static const char *exitcmd[] = { "pkill", "awm", NULL  };
static Key keys[] = {
	{ MODKEY,	XK_Return,		spawn,	{.v = termcmd} },
	{MODKEY, XK_c, killclient, {0}},
	{ MODKEY,       XK_q,                   quit,           {0}     },
};
